id: io.gitlab.azymohliad.WatchMate
runtime: org.gnome.Platform
runtime-version: '42'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: watchmate
finish-args:
  - --allow=bluetooth
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --system-talk-name=org.bluez
  - --talk-name=org.mpris
modules:
  - name: watchmate
    sources:
      - type: dir
        path: ..
    buildsystem: simple
    build-commands:
      - cargo build --release --locked
      - install -Dm 755 target/release/watchmate -t /app/bin
      - install -Dm 644 assets/${FLATPAK_ID}.desktop -t /app/share/applications
      - install -Dm 644 assets/${FLATPAK_ID}.metainfo.xml -t /app/share/metainfo
      - install -Dm 644 assets/icons/${FLATPAK_ID}.svg -t /app/share/icons/hicolor/scalable/apps
      - install -Dm 644 assets/icons/${FLATPAK_ID}-symbolic.svg -t /app/share/icons/hicolor/symbolic/app
    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      build-args:
        - --share=network
